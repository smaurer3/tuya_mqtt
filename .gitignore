# Tuya credentials and per-device local keys — must never be committed
devices.json
snapshot.json
tinytuya.json
tuya-raw.json

# Per-deployment configuration — user state, not source code.
# Apps load defaults from code if these are missing on a fresh deploy.
aliases.json
gpio_config.json
mqtt_config.json
admin_config.json
pdu_config.json

# Python
__pycache__/
*.pyc
*.pyo

# Editor
.vscode/
.idea/
*.swp

# Claude Code local state
.claude/
